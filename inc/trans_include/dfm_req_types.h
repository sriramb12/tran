#ifndef dfm_req_types_h 
#define dfm_req_types_h

/* List of defined DFM request types */
typedef enum
{
    DFMJOB_SUBMIT,	/* Submit a new DFM job */
    DFMJOB_CANCEL,	/* Cancel an existing DFM job */
    DFM_CONTROL_FILE_GET,
    DFM_TECHNOLOGY_GET,
    DFM_FILE_GET,
    CONTROL_FILE_PUT,
    TECHNOLOGY_FILE_PUT,
    CONTROL_FILE_LIST,
    TECHNOLOGY_FILE_LIST,
    DFM_MKDIR,	/* Send an DFM directory */
    RET_RMDIR,	/* Delete an existing directory on the RET machine */
    RETDIR_ADD_FILES,	/* Add files to an existing directory on the RET machine */
    RETDIR_DLT_FILES,	/* Delete files from a directory on the RET machine */
    RETDIR_GET_FILE,	/* Get specified file from RET machine */
    RETDIR_GET_FILES,	/* Retrieve RET job output files from RET machine */
    RETDIR_LIST,	/* List files in directory on RET machine */
    RETDIR_LIST_ROOTS,	/* List RET roots present on the RET machine */
    RET_PS_SHOW,	/* Show work load on RET machine */
    DFMQ_GET,
    DFMQ_AUDIT,
    DFM_AUTOREG,
    DFM_CHECKREG,
    CLEANUP,
    AUDIT,
    RUN_NEW_JOB,
    ANDY_CLEANUP,
    DFM_AUTH_CHECK,
    DFM_CONTROL_FILE_GET_PRIOR,
    DFM_TECHNOLOGY_GET_PRIOR,
    DFM_GTAR_GET,
} RET_REQ_TYPE;


#endif /* Define for dfm_req_types_h */
#ifndef dfm_req_types_h 
#define dfm_req_types_h

/* List of defined RET request types */
typedef enum
{
    DFMJOB_SUBMIT,	/* Submit a new RET job */
    DFMJOB_CANCEL,	/* Cancel an existing RET job */
    DFM_CONTROL_FILE_GET,
    DFM_TECHNOLOGY_GET,
    DFM_FILE_GET,
    CONTROL_FILE_PUT,
    TECHNOLOGY_FILE_PUT,
    CONTROL_FILE_LIST,
    TECHNOLOGY_FILE_LIST,
    DFM_MKDIR,	/* Send an RET directory */
    RET_RMDIR,	/* Delete an existing directory on the RET machine */
    RETDIR_ADD_FILES,	/* Add files to an existing directory on the RET machine */
    RETDIR_DLT_FILES,	/* Delete files from a directory on the RET machine */
    RETDIR_GET_FILE,	/* Get specified file from RET machine */
    RETDIR_GET_FILES,	/* Retrieve RET job output files from RET machine */
    RETDIR_LIST,	/* List files in directory on RET machine */
    RETDIR_LIST_ROOTS,	/* List RET roots present on the RET machine */
    RET_PS_SHOW,	/* Show work load on RET machine */
    DFMQ_GET,
    DFMQ_AUDIT,
    DFM_AUTOREG,
    DFM_CHECKREG,
    CLEANUP,
    AUDIT,
    RUN_NEW_JOB,
    ANDY_CLEANUP,
    DFM_AUTH_CHECK,
    DFM_CONTROL_FILE_GET_PRIOR,
    DFM_TECHNOLOGY_GET_PRIOR,
} RET_REQ_TYPE;


#endif /* Define for dfm_req_types_h */
#ifndef dfm_req_types_h 
#define dfm_req_types_h

/* List of defined RET request types */
typedef enum
{
    DFMJOB_SUBMIT,	/* Submit a new RET job */
    DFMJOB_CANCEL,	/* Cancel an existing RET job */
    DFM_CONTROL_FILE_GET,
    DFM_TECHNOLOGY_GET,
    DFM_FILE_GET,
    CONTROL_FILE_PUT,
    TECHNOLOGY_FILE_PUT,
    CONTROL_FILE_LIST,
    TECHNOLOGY_FILE_LIST,
    DFM_MKDIR,	/* Send an RET directory */
    RET_RMDIR,	/* Delete an existing directory on the RET machine */
    RETDIR_ADD_FILES,	/* Add files to an existing directory on the RET machine */
    RETDIR_DLT_FILES,	/* Delete files from a directory on the RET machine */
    RETDIR_GET_FILE,	/* Get specified file from RET machine */
    RETDIR_GET_FILES,	/* Retrieve RET job output files from RET machine */
    RETDIR_LIST,	/* List files in directory on RET machine */
    RETDIR_LIST_ROOTS,	/* List RET roots present on the RET machine */
    RET_PS_SHOW,	/* Show work load on RET machine */
    DFMQ_GET,
    DFMQ_AUDIT,
    DFM_AUTOREG,
    DFM_CHECKREG,
    CLEANUP,
    AUDIT,
    RUN_NEW_JOB,
    ANDY_CLEANUP,
    DFM_AUTH_CHECK,
    DFM_CONTROL_FILE_GET_PRIOR,
    DFM_TECHNOLOGY_GET_PRIOR,
} RET_REQ_TYPE;


#endif /* Define for dfm_req_types_h */
